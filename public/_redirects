# Static files must be served directly — list them before the SPA catch-all
/sitemap.xml    /sitemap.xml    200
/robots.txt     /robots.txt     200
/llms.txt       /llms.txt       200
/ads.txt        /ads.txt        200

# SPA rewrites — /m uses /index.html (no dist/m/ folder) to avoid 308 → /m/
/m      /index.html     200
/m/     /index.html     200
/:path  /index.html     200
